sudo: false

language: python
python: "2.7"

install:
    - pip install ansible

before_script:
    - ansible-galaxy install -r roles.txt

script:
    - ansible-playbook -i production --check playbook.yml

addons:
    apt:
        packages:
            - python-apt
            - python-pycurl
